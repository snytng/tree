# 開発ログ

## 1. プロトタイプ初期フェーズ (2024-XX-XX)

### 1.1 プロジェクトの開始
React Flowを使用して「ホワイトボードのように自由で、かつグラフDBのような意味論を持つ」モデリングツールのプロトタイプを作成開始。

### 1.2 初期モックの動作確認
Vite + React + React Flowで、ノード（四角）とエッジ（線）を動的に追加・接続できる最小構成のモックが動作済み。
ユーザーは「最初はスキーマレスに、徐々に構造化（属性定義）」していく成長型のツールを目指している。

### 1.3 共同編集機能の検討
次のステップとして、このプロトタイプを「共同編集（リアルタイム同期）」に対応させることを決定。

#### 1.3.1 技術スタックの選定
- 同期エンジン: Yjs (CRDT) を使用。
- 通信: まずはサーバー不要でブラウザ間通信ができる y-webrtc 、または簡単なバックエンドで済む y-websocket を想定。

#### 1.3.2 実装要件
- nodes と edges の状態を Yjs の Y.Doc で管理し、複数タブ/ブラウザ間でリアルタイムに同期させる。
- React Flow の onNodesChange や onEdgesChange を Yjs の共有型（Shared Types）と連動させる。

#### 1.3.3 実装依頼
この構成を実現するための、App.jsx の修正案と必要なライブラリのインストールコマンドを依頼。

## 2. ドキュメント連携とプロジェクト管理フェーズ (2024-XX-XX)

### 2.1 ドキュメント・トレーサビリティの実装
- `spec.md` および `design.md` から ID `[S-xxx]`, `[D-xxx]` を動的に抽出するパーサーを実装。
- `mapping.json` に基づき、要件と設計の依存関係をキャンバス上に自動構築する機能を導入。

### 2.2 プロジェクト入出力 (ZIPパッケージング)
- `jszip` を導入し、Markdownファイル群と `mapping.json` を一括でインポート/エクスポートする機能を実装。
- インポート時にプロジェクト名を自動識別し、画面上に表示・同期するメタデータ管理を導入。
- 自動レイアウト（Dagre）とカメラ制御（fitView）の連動により、インポート直後の視認性を向上。

### 2.3 操作性と視認性の向上フェーズ (2024-XX-XX)
- **[D-014] キーボードナビゲーションの実装**: 
    - 矢印キーで視覚的に最も近いノードへフォーカスを移動するカスタムフック `useKeyboardNavigation` を導入。
    - 軸のズレに重みを付けた距離計算ロジックを採用し、直感的な移動を実現。
- **[D-015] ノード形状の安定化**:
    - ノードサイズを `180x60` に固定。自動レイアウト時の計算の不確実性を排除。
- **レイアウトアルゴリズムのブラッシュアップ**:
    - 再帰的領域スタッキングにおいて、親ノードが常に子ノード群の垂直方向中央に配置されるよう計算式を修正。
- **プロジェクト構造の整理**:
    - Reactの流儀に従い、カスタムフックを `src/hooks/` ディレクトリへ配置。

### 2.4 構造編集機能の検討 (2024-XX-XX)
- **[B-004] 構造的ツリー編集の提案と吟味**:
    - キーボードによる親子関係の組み換え、および兄弟間の順序変更の仕様を策定。
    - `backlog.md` への追加と、開発ルールの定義を更新。
- **[S-016] 兄弟の順序入れ替え実装**:
    - `Ctrl + ArrowUp/Down` による兄弟ノードの垂直順序変更機能を実装。
    - 座標のスワップにより自動レイアウトの並び順を制御。

### 2.5 バグ修正 (2024-XX-XX)
- **ノード削除の即時反映修正 [D-018]**: `onDeleteSelected` のトランザクションオリジンを `'local'` から `'structural'` に変更し、自分自身の削除操作が即座に UI に反映されない問題を修正。
- **同期オリジンの統一調整**:
    - `onConnect`、`onAddNode`、`onAddStructuredNode` のオリジンを `'structural'` に変更。
    - `onNodesChange` / `onEdgesChange` において、要素の削除が含まれる場合のみ自動的に `'structural'` オリジンを使用するようロジックを追加。

### 2.6 ノード編集機能の実装 (2026-04-28)
- **[B-001] インライン編集の導入**:
    - `CustomNode` に編集モード（`isEditing`）を追加し、ダブルクリックまたは `F2` で起動。
    - React Flow の `zoomOnDoubleClick` を無効化し、ダブルクリックイベントの競合を解消。
    - 入力中のキーイベント伝播を停止し、矢印キーによるノード移動を抑制。
    - 編集確定時に `yNodes` および `projectFiles` (Markdown) の該当ヘッダーを同期更新する `useNodeEditor` フックを実装。

### 2.7 エッジ追加モードの実装 (2026-04-28)
- **[B-005] クリックによるエッジ追加モードの導入**:
    - `isEdgeMode` と `edgeSourceId` ステートを `App.jsx` に追加。
    - 「エッジ追加」ボタンでモードをトグルし、ノードクリックで接続元/接続先を決定。
    - `onNodeClick` と `onPaneClick` をオーバーライドし、エッジ作成ロジックを実装。
    - `Escape` キーでモード解除および選択状態をリセット。
    - 接続元ノードに視覚的なフィードバック（青枠）を提供。
    - 接続完了後、接続先ノードに自動的にフォーカスが移動するよう改善。
    - これにより、構造変更を伴う全操作で `syncState` による再レイアウトが確実に行われるよう改善。

### 2.8 構造的レイアウトの安定化向上 (2026-04-28)
- **[D-004] 論理的中間座標挿入の実装**:
    - ノード挿入時（Enter/Insert）に、表示上の座標ではなく Yjs の共有データ上の「次」のノードとの中間 Y 座標を計算して割り当てるロジックを導入。
    - これにより、自動レイアウトが有効な状態でも、選択したノードの「直下」に確実に新規ノードが挿入されるよう改善。
    - インポート時や初期配置時にも微小なオフセットを付与し、ソート順の非決定性を排除。

### 2.9 上方向へのノード挿入機能の実装 (2026-04-28)
- **[B-012] Shift + Enter による「上へ挿入」の導入**:
    - `onAddStructuredNode` に `sibling-above` モードを追加。
    - 選択ノードの直前の兄弟ノードとの中間座標を計算して割り当てるロジックを実装。
    - キーボードイベントで `Shift + Enter` を検知して実行するように設定。

### 2.10 左方向（親方向）へのノード挿入機能の実装 (2026-04-28)
- **[B-013] Shift + Insert による「左へ挿入」の導入**:
    - `onAddStructuredNode` に `parent` モードを追加。
    - 既存の親エッジを切断し、新ノードを間に割り込ませる「アトミック・スプライス（繋ぎ変え）」ロジックを実装。
    - 複数の親を持つ場合も全ての経路に新ノードを挿入するよう処理。

## 2.11 レイアウト密度の調整 (2026-04-28)
- **[S-015] 水平間隔の最適化**:
    - 水平方向のノード間隔が広すぎたため、`horizontalStep` をノード幅と同じ `180` に変更。
    - これに合わせて、左方向（親方向）への挿入時のオフセット（[D-021]）も `-180` に調整。
    - これにより、ノードが水平方向に隙間なく配置される、よりコンパクトなツリー構造を実現。

## 2.12 関連ノード抽出表示（フォーカスモード）の実装完了 (2026-04-28)
- **[B-016] の実装と検証**:
    - 選択ノードを起点としたグラフ探索（祖先・子孫）によるフィルタリングロジックを実装。
    - React Flow の `hidden` プロパティと自動レイアウトの動的除外を連携させ、フォーカス対象を画面中央に再配置する挙動を実現。
    - `F` キーおよびツールバーボタンによる操作インターフェースを統合。
    - ユーザーによる実機テストをパスし、期待通りの動作（フィルタリングおよび解除時の整合性）を確認。

## 2.13 ドキュメントと実装の完全同期 (2026-04-28)
- **整合性チェック**: `App.jsx` 内のレイアウト定数を `design.md` ([D-004], [D-015]) の定義値（180px, 40px）に完全に同期。
- **設計書のクリーンアップ**: `design.md` 内の誤ったセクション階層を修正。

## 2.14 レイアウト密度の再調整 (2026-04-28)
- **[S-015] 水平間隔の変更**:
    - 視認性向上のため、`horizontalStep` を `180` から `360` に変更。
    - ノード挿入時の相対オフセットを `-360` に更新し、自動レイアウトとの整合性を維持。

## 2.15 MCPサーバー連携の着手 (2026-04-28)
- **[B-018] 要件定義と設計**:
    - AI が図面を操作するための MCP ツール群（CRUD）を定義。
    - Yjs を介したブラウザ・サーバー間のリアルタイム同期アーキテクチャ（y-websocket 経由）を設計。

## 2.16 MCPサーバーの実装完了 (2026-04-28)
- **[B-018] 実装フェーズ**:
    - `App.jsx` に `WebsocketProvider` を導入し、ブラウザと外部プロセスの同期経路を確立。
    - `mcp-server/index.js` を作成し、`read_graph`, `add_node`, `connect_nodes` ツールを実装。

## 2.17 セキュリティアップデート: MCP SDK (2026-04-28)
- **[B-018] 依存関係の更新**:
    - `@modelcontextprotocol/sdk` に発見された高危険度脆弱性 (ReDoS, DNS rebinding protection) に対応するため、`mcp-server/package.json` 内のバージョンを `^0.6.0` から `^1.29.0` へ更新。
    - この更新は破壊的変更を含む可能性があるため、更新後にMCPサーバーの動作確認が必要です。
    - 修正適用後、`mcp-server` ディレクトリで `npm install` を実行し、依存関係を更新してください。

## 2.18 Gemini CLI環境の整備 (2026-04-28)
- **環境設定**:
    - MCPサーバーのテストおよびAIアシスタント連携のため、Google AI StudioよりGemini APIキーを取得。
    - システム環境変数 `GOOGLE_API_KEY` を設定し、CLIからの接続準備を完了。

## 2.19 y-websocket 実行エラーの修正 (2026-04-28)
- **原因**: `npx y-websocket` がルートディレクトリで実行された際、`y-websocket` が `mcp-server/package.json` の依存関係にしか存在しなかったため、実行可能ファイルが見つからなかった。
- **修正**: プロジェクトのルートディレクトリの `package.json` に `y-websocket` を直接依存関係として追加。
- **対応**: `npm install` をルートディレクトリで再実行することで、`y-websocket` コマンドが `npx` から利用可能になる。

## 2.20 複数コマンド同時実行とサーバーデプロイガイドライン (2026-04-28)
- **[B-018] 開発効率化**:
    - 開発環境でYjs同期サーバー、フロントエンド、MCPサーバーを同時に起動するため、`concurrently` を導入し、`npm run start:all` スクリプトを追加。
- **[B-018] サーバーデプロイガイドライン**:
    - フロントエンドは `npm run build` で静的ファイルを生成し、Webサーバーでホスティングする方針を提示。
    - Yjs同期サーバー (`y-websocket`) および MCPサーバー (`mcp-server/index.js`) は、`pm2` や `systemd` などのプロセス管理ツールを用いてバックグラウンドで永続的に実行する方針を提示。
    - サーバーのファイアウォール設定やドメイン設定など、ネットワークに関する考慮事項を明記。
    - `USAGE.md` にこれらの手順とガイドラインを追記。

## 2.22 プロジェクト名編集機能の着手 (2026-04-28)
- **[B-010] 要件定義と設計**:
    - プロジェクト名をインライン編集できる機能の仕様を策定し、`spec.md` および `design.md` に反映。

## 2.23 プロジェクト名編集機能の実装完了 (2026-04-28)
- **[B-010] 実装と検証**:
    - プロジェクト名パネルをクリックで編集モードに移行し、Yjs経由で同期する機能を実装。

## 2.24 構造的階層変更機能（Ctrl + Right）の着手 (2026-04-28)
- **[B-004] の要件定義と設計**:
    - 選択ノードを直上の兄弟の子にするインデント機能の仕様を策定。

## 2.25 構造的階層変更機能（Ctrl + Right）の実装完了 (2026-04-28)
- **[B-004] 実装フェーズ**:
    - `useKeyboardNavigation` フックに `Ctrl + ArrowRight` ハンドラを追加し、エッジの張り替えによる階層下げを実現。

## 2.26 構造的階層変更機能（Ctrl + Left）の実装完了 (2026-04-28)
- **[B-004] 実装フェーズ**:
    - `useKeyboardNavigation` フックに `Ctrl + ArrowLeft` ハンドラを追加。
    - 親から切り離し、祖父（存在すれば）に接続、またはルート化する「アウトデント（階層上げ）」を実現。
    - 元の親の直後に配置されるよう Y 座標を微調整 (`+0.01`) し、論理的な順序を維持。

## 2.27 ルートノードの順序入れ替え対応 (2026-04-28)
- **[D-016] の拡張**:
    - `Ctrl + ArrowUp/Down` において、親ノードがない（ルート）場合でも、他のルートノードを兄弟とみなして順序を入れ替えられるようロジックを修正。
    - これにより、ツリーの根本レベルでの並び替えが可能になった。

## 2.28 開発フェーズの区切りとルール更新 (2026-04-28)
- **完了確認**: [B-004] 構造的ツリー編集ショートカットの全項目（順序入れ替え、階層上げ、階層下げ、ルートノード対応）の実装と検証を完了。
- **ルール更新**: `development_rules.md` に「バックログの自動進行禁止」を明記。タスク完了後に AI が勝手に次へ進まないよう制御を強化。

## 2.29 ノード直接移動機能の着手 (2026-04-28)
- **[B-008] 要件定義と設計**:
    - ドラッグ＆ドロップによるノードの親子関係変更、およびエッジへの割り込み機能の仕様を策定。
    - 循環参照の防止や、自動レイアウトとの連携ロジックを設計。
    - `spec.md` および `design.md` に反映。

## 2.30 ノード直接移動機能の実装完了 (2026-04-28)
- **[B-008] 実装フェーズ**:
    - `onNodeDragStart`, `onNodeDrag`, `onNodeDragEnd` ハンドラを実装。
    - `getIntersectingNodes` / `getIntersectingEdges` を利用したドロップターゲット検出ロジックを構築。
    - 循環参照（子孫ノードへのドロップ）を防止する `isDescendant` チェック関数を導入。
    - `structural` トランザクションによるエッジの動的張り替えと自動レイアウトの連動を確認。

## 2.31 ノード直接移動のバグ修正と安定化 (2026-04-28)
- **事象**: ドラッグ＆ドロップ時にエッジの張り替えが発火しない、または手動モードで座標が戻る問題が発生。
- **原因特定**: 
    1. React Flow v11 のイベント名が `onNodeDragStop` であるべきところが `onNodeDragEnd` になっていた。
    2. 手動モード時にノードの `width`/`height` 情報が欠落し、交差判定 (`getIntersectingNodes`) が失敗していた。
    3. 交差判定がデフォルトで完全包含を求めていた。
- **解決策**: イベント名の修正、`syncState` での固定サイズ注入、判定時の `partially=true` オプション導入により、手動・自動両モードでの安定動作を確認。

## 2.32 接続解除機能の実装 (2026-04-28)
- **[S-027] の導入**:
    - ノードを「何もない場所」へドロップした際に親との接続を自動解除する機能を実装。
    - ドラッグ＆ドロップのロジックに「ターゲットなし」時のエッジ削除処理を追加し、直感的なルート化を実現。

## 2.33 ドラッグ操作の安全性向上とモード分離 (2026-04-28)
- **[S-028] の実装**:
    - 意図しないエッジの削除や繋ぎ変えを防止するため、構造変更ロジックのトリガーを「Shift + ドラッグ」に限定。
    - 通常ドラッグは純粋なレイアウト調整（手動モード時）のみに機能を縮小。
    - 自動レイアウト ON 時でも Shift + ドラッグによる構造編集を許可するよう `nodesDraggable` 設定を調整。

## 2.34 自動レイアウトON時のドラッグ後のスナップバック調整 (2026-04-28)
- **修正内容**: 自動レイアウトが有効な際、通常のドラッグで移動したノードがドロップ位置に残ってしまう問題を解決。
- **対応**: `onNodeDragStop` において、自動レイアウトON時は常に `structural` オリジンを使用し、かつ `yProjectMeta` をダミー更新することで再レイアウトを強制。
- **結果**: ドラッグ終了時に計算済みの正しい座標へノードが即座に復帰する挙動を確認。

## 2.35 構造編集の安全性向上と順序入れ替えの直感化 (2026-04-28)
- **[S-027] 仕様変更**: 「何もない場所へのドロップで接続解除」という強力すぎる挙動を撤回。
- **[S-028] 拡張**: Shiftドラッグ中にソースノード（ドラッグ中のノード）を薄い赤色でハイライトし、「構造編集モード」であることを視覚化。
- **[S-029] 新機能**: Shiftドラッグでノード間にドロップした際、その物理座標を Yjs に保存し、自動レイアウト側で兄弟間のソート順（1, 3, 2 等）として反映させるロジックを実装。
- **結果**: 意図しないエッジ削除を防ぎつつ、マウスによる直感的な並び替えが可能になった。

## 2.36 ドラッグ操作の安定化と用語整理 (2026-04-28)
- **用語定義**: 「構成 (Structure)」モード (Shiftドラッグ) と 「配置 (Layout)」モード (通常ドラッグ) を定義。
- **チャタリング対策**: `onNodeDrag` において、前回のハイライト対象を `useRef` で保持し、変化時のみクラスを操作することで点滅を解消。
- **キー判定修正**: `useEffect` 内のキー監視において、Shiftキーが離された瞬間に即座にハイライトを消去する `clearHighlights` 処理を導入。
- **バグ修正**: 構成モードから配置モードへドラッグ中に切り替えた際、残像が残る問題を解決。

## 2.37 セレクションのローカル化とドラッグ操作の最終調整 (2026-04-28)
- **[S-030] 実装**: 選択状態（selected）を Yjs の共有データから除外し、各ユーザーのブラウザで独立して管理するよう修正。共同編集時のフォーカス競合を解消。
- **[S-027] 仕様再定義**: 自動レイアウト ON 時でも、通常のドラッグ（配置モード）でノードを移動させた際にその座標を保存し、兄弟間の順序入れ替えに反映する挙動を実装。
- **操作のシンプル化**: 構成モード（Shiftドラッグ）を接続変更（子ノード化・エッジ挿入）に特化させ、背景ドロップによる接続解除仕様を廃止。
- **スタイルの安定化**: CSS セレクタ（`.structure-mode-active`）によるステート駆動型のハイライト制御に移行し、レンダリングと DOM 操作の不整合を完全に解消。

## 2.38 構造編集機能群の完了 (2026-04-28)
- **ステータス更新**: [B-008] ノードの直接移動、[B-004] 構造的ショートカットを含む一連のツリー編集機能の実装・検証をすべて完了。
- **リファクタリングの検討**: `App.jsx` の肥大化に伴い、将来的な `layoutEngine.js` 等への責務分散案を設計書 [D-028] に記録。

## 2.39 レイアウトエンジンの分離リファクタリング (2026-04-28)
- **[D-028] 実装**: `App.jsx` 内に定義されていた自動レイアウトロジック `getLayoutedElements` を `src/utils/layoutEngine.js` に抽出。
- **目的**: UIコンポーネントの責務を軽減し、レイアウトロジックの再利用性とメンテナンス性を向上。
- **結果**: `App.jsx` のコード量を削減し、論理的なディレクトリ構造を整理。

## 2.40 ゴーストノード（レイアウトプレビュー）の着手 (2026-04-28)
- **[B-008] の拡張設計**:
    - 自動レイアウト時の「スナップバック」による混乱を軽減するため、ドロップ後の位置を事前に可視化する「ゴーストノード」機能を設計。
    - ドラッグ中の動的レイアウト計算と、視覚的なフィードバック方法を `design.md` に定義。

## 2.41 ゴーストノードの実装完了 (2026-04-28)
- **[B-014/S-031] 実装**:
    - `onNodeDrag` 内でドラッグ座標を元にした仮のレイアウト計算を間欠実行するロジックを実装。
    - 計算された確定位置を半透明の点線枠（ゴースト）として表示。
    - これにより、縦に並んだ兄弟ノードの間など、狙った位置へのドロップが容易になった。

## 2.42 論理スロット・スナップへの配置統合 (2026-04-28)
- **[D-029] 実装の刷新**:
    - 全計算方式から「兄弟間の中間座標（スロット）」への吸着方式へ変更。
    - ドラッグ中に最も近いノード間の隙間にゴーストが固定されるようになり、表示の「暴れ」を解消。
    - ドロップ時に中間座標を保存する仕組みにより、プレビューと自動レイアウト結果の完全な一致を保証。
    - 自動レイアウト OFF->ON 時のロジックとドラッグ操作の内部処理を「相対座標による順序管理」として一本化。

## 2.43 ライブ・ローカルレイアウトへの刷新 (2026-04-28)
- **[D-029] 仕様変更**: 視覚的な不整合があった「ゴーストノード」を廃止。
- **新アプローチ**: ドラッグ中、ローカルの React State 内で全ノードをリアルタイムに再配置するロジックを実装。
- **結果**: ドラッグしているノードの動きに合わせて周囲のノードが自然に避ける、または入れ替わる挙動を実現。ドロップ時の正確な位置確定を保証。
    - **修正**: ドロップ時にドラッグしたノードだけでなく、影響を受けた全ノードの座標を Yjs にコミットすることで、再計算時のソート順の不整合（ストレッチバック）を完全に解消。

## 2.44 クリップボードからのノード追加機能の着手 (2026-04-28)
- **[B-022] 要件定義と設計**:
    - クリップボードのテキスト内容を解析し、ノードとしてキャンバスに追加する機能の仕様を策定。
    - `Ctrl + V` キーイベントのハンドリング、ノードの初期配置ロジック、Yjs同期について設計。
    - `spec.md` および `design.md` に反映。

## 2.45 階層テキストのコピペ機能の設計 (2026-04-28)
- **[B-022] の拡張設計**:
    - 単一ノードだけでなく、インデントされたテキストを解析してツリー構造を生成する [S-033] を策定。
    - `Ctrl + C` で選択ノード配下をテキスト出力する [S-034] を策定。
    - スタックを用いた階層解析アルゴリズム [D-031] および DFS によるテキスト変換 [D-032] を設計。
    - これにより、Excelやアウトライナーとの双方向連携が可能になる。

## 2.46 階層テキストのコピペ機能の実装完了 (2026-04-28)
- **[B-022/S-032-034] 実装と検証**:
    - 改行コードの差異や特殊文字に強い階層テキストパーサーを `graphUtils.js` に実装。
    - 選択ノードを起点としたサブツリーのテキスト化（コピー）と、インデント解析による構造復元（ペースト）の双方向連携を確認。
    - 大規模な階層データの取り込み時も、自動レイアウトと連動して正しく整列されることを検証。
    - リファクタリングにより `App.jsx` から共通ロジックを分離し、メンテナンス性を向上。