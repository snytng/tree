# My Diagram App バックログ (Backlog)

## 現在の注力タスク (Active Task)
 (None - ユーザーからの次の指示待ち)

## ステータス定義
- [ ] **Todo**: 未着手。要件定義や設計から開始する必要がある。
- [R] **Ready**: 準備完了。テスト仕様が定義され、ユーザーの確認が済んだ状態。
- [/] **Doing**: 進行中。現在実装または設計の詰めを行っている。
- [T] **Testing**: 実装は完了し、ユーザーによる実機テスト・承認待ちの状態。
- [x] **Done**: 実装・テスト完了。

---

## 43. [x] [B-043] MCPサーバーのマルチプロジェクト・マルチ図面対応 - Done
- **概要**: MCPサーバーが固定ルーム (`react-flow-demo-room`) にしか接続できず、B-033（マルチプロジェクト）・B-038（ブロック図ビュー）で追加されたデータ構造にアクセスできない問題を解消する。
- **背景**: B-033 でフロントエンドのルーム名が `mda_{projectId}` に変更されたにもかかわらず、MCPサーバー側が旧ルーム名のまま取り残された。影響分析ルールの不在により検出が遅れた。
- **要求仕様**: [S-055], [S-056], [S-057]
- **設計**: [D-058] マルチプロジェクト接続管理, [D-059] ブロック図ツール追加
- **設計方針の要点**:
    - ~~`list_projects` は提供せず、AIがprojectIdを直接指定する方式~~（設計変更: プロジェクトレジストリ経由で list_projects を提供。ID を直接指定する方式は AI が推測不可能なため）
    - 既存5ツール (read_graph, add_node, connect_nodes, update_node, delete_node) は維持
    - 新規9ツール追加: list_projects, switch_project, current_project, list_diagrams, read_diagram, add_node_to_diagram, add_edge_to_diagram, get_project_name, update_project_name
- **検出経緯**: ドキュメント整備後のレビューでユーザーが気づいた。ルール違反レポート (2.93) 参照。

## 33. [x] [B-033] マルチプロジェクト対応 - Done
- **概要**: 複数のプロジェクトをlocalStorageベースで管理し、プロジェクトごとに独立したYjsルーム・IndexedDB永続化を行う機能。
- **実装内容**:
    - `projectStore.js`: プロジェクトCRUD、アクティブプロジェクト管理、ルーム名生成、タブ状態の保存/復元
    - `initYjsForProject(projectId)`: プロジェクト切り替え時のYjsプロバイダー再初期化
    - `switchProject()`: プロジェクト切り替えコールバック（タブ状態保存→Yjs再初期化→タブ状態復元）
    - HMR時のYjsプロバイダークリーンアップ (`import.meta.hot.dispose`)

## 44. [x] [B-044] プロジェクト作成機能の安定化とリセット機能 - Done
- **概要**: プロジェクトが0件の状態からでも新規作成が正常に動作するように修正。また、開発環境のデータを完全に初期化するためのリセットコマンドを実装する。
- **背景**: プロジェクト作成時の非同期処理の考慮漏れや、CORS設定の問題により、特定の条件下でプロジェクトが作成できない不具合があった。
- **要求仕様**: [S-058]
- **設計**: [D-060]

## 33. [x] [B-033] マルチプロジェクト対応 - Done
- **概要**: 複数のプロジェクトをlocalStorageベースで管理し、プロジェクトごとに独立したYjsルーム・IndexedDB永続化を行う機能。
- **実装内容**:
    - `projectStore.js`: プロジェクトCRUD、アクティブプロジェクト管理、ルーム名生成、タブ状態の保存/復元
    - `initYjsForProject(projectId)`: プロジェクト切り替え時のYjsプロバイダー再初期化
    - `switchProject()`: プロジェクト切り替えコールバック（タブ状態保存→Yjs再初期化→タブ状態復元）
    - HMR時のYjsプロバイダークリーンアップ (`import.meta.hot.dispose`)

## 34. [x] [B-034] グローバルコマンドバー (GlobalBar) - Done
- **概要**: Linear/Figma風のグローバルコマンドバーを画面上部に配置。プロジェクト名表示・編集、図を開く、図を追加、ビュー追加ドロップダウンを統合したUI。
- **実装内容**:
    - `GlobalBar`コンポーネント: プロジェクト名（クリック=ブラウザ、ダブルクリック=編集）、「図を開く」「図を追加」「ビュー追加▾」ボタン
    - 旧Panel (top-left) のプロジェクト名表示を削除し、GlobalBarに移行

## 35. [x] [B-035] プロジェクト/ダイアグラムブラウザダイアログ - Done
- **概要**: プロジェクト一覧やダイアグラム一覧をモーダルダイアログで閲覧・選択・リネーム・削除できるUI。
- **実装内容**:
    - `BrowserDialog.jsx`: 汎用モーダルブラウザダイアログ（検索、リネーム、削除確認）
    - `ProjectBrowserDialog.jsx`: プロジェクト一覧用
    - `DiagramBrowserDialog.jsx`: ブロック図一覧用

## 36. [x] [B-036] タブバーとマルチビュー対応 - Done
- **概要**: VS Code風タブバーを実装し、ノードグラフ・一覧表・ブロック図・プレースホルダなど複数ビューをタブで切り替えられる機能。
- **実装内容**:
    - `TabBar.jsx` / `TabBar.css`: タブ表示・切り替え・ダブルクリックリネーム・ドラッグ並び替え・右クリックコンテキストメニュー（閉じる/これ以外を閉じる/すべて閉じる）
    - `VIEW_TYPES`定義: node-graph, table, block-diagram, function-flow
    - `App.jsx`: タブ状態管理（tabs/activeTabId）、`closeTab`/`reorderTab`/`closeOtherTabs`/`closeAllTabs`コールバック
    - タブ状態のlocalStorage永続化

## 37. [x] [B-037] 一覧表ビュー (TableView) - Done
- **概要**: ノードグラフのデータをExcel風スプレッドシートで閲覧・操作できるテーブルビュー。
- **実装内容**:
    - `TableView.jsx`: Yjs連携テーブル、右クリックメニュー（「図を開いてフォーカス」含む）
    - `App.jsx`: `handleOpenDiagramFromTable`でテーブルから特定ノードへのフォーカス遷移

## 38. [x] [B-038] ブロック図ビュー (BlockDiagramView) - Done
- **概要**: ノードグラフの要素を自由配置で視覚化するブロック図ビュー。矩形・ひし形・楕円等のシェイプ、色・サイズ変更、エッジ接続をサポート。
- **実装内容**:
    - `BlockDiagramView.jsx` / `BlockDiagramView.css`: ブロック図のReactFlowベースキャンバス
    - `ShapeNode.jsx`: 矩形・ひし形・角丸矩形・楕円・六角形の描画
    - `FloatingEdge.jsx`: ノード境界からの浮動エッジ
    - Yjsマップ `bdLayout_${diagramId}` / `bdEdges_${diagramId}` によるデータ管理
    - `bdDiagramsMeta`によるダイアグラムメタ情報管理
    - フォーカスノード対応（リトライ付きsetCenter）

## 39. [x] [B-039] ノードグラフ右クリック・選択メニュー - Done
- **概要**: ノードグラフで複数ノードを選択した状態で右クリックまたは左クリックすると操作メニューを表示する機能。メニューから「ブロック図を新規作成」「既存ブロック図に追加」を実行可能。
- **実装内容**:
    - `NodeGraphContextMenu`コンポーネント: メニューUI、サブメニュー（既存ブロック図一覧）、画面端の位置調整
    - `onNodeContextMenu` / `onPaneContextMenu` / `onSelectionContextMenu`: 3つの右クリックハンドラ
    - `onNodeClickWithMenu`: 複数選択中の左クリックでもメニュー表示
    - `onSelectionChangeForMenu` + `prevSelectedNodeIdsRef`: ReactFlowの選択リセット問題への対応
    - カスタムイベント `ngCreateDiagramWithNodes` / `ngOpenDiagramTab` でFlow→App間通信

## 40. [x] [B-040] ノード追加時のクラス継承と固定ラベル - Done
- **概要**: Ins/Enterで追加したノードが選択中ノードの`data.nodeClass`を継承する。ラベルは常に「New Node」固定。
- **実装内容**:
    - `onAddStructuredNode`: `baseClass`を選択ノードから取得しnewNodeに設定
    - ラベルを`'New Node'`に統一（旧: `Child of ...` / `Sibling of ...`）

## 41. [x] [B-041] ノード作成直後の自動編集モード - Done
- **概要**: ノード選択中に文字キーを押すと自動的にインライン編集モードに入り、押した文字が初期値として入力される機能。
- **実装内容**:
    - `CustomNode.jsx`: `onKeyDown`ハンドラで英数字キー検出→編集モード開始（Ctrl/Alt/Meta/スペース除外）

## 42. [x] [B-042] デバッグ情報の強化 - Done
- **概要**: UI操作イベントのリングバッファログ(`[RF-Debug]`)を導入し、デバッグ情報コピーボタンの出力に直近20件のイベント履歴を含める機能。
- **実装内容**:
    - `rfDebug()`関数: コンソールログ＋リングバッファ蓄積（最大50件）
    - `_rfDebugLog`: イベント履歴リングバッファ
    - `onCopyDebugInfo`: タイムスタンプ、選択状態、ノード/エッジのselectedフラグ、recentEventsを出力
    - `RF_DEBUG`フラグでログ出力のON/OFF切り替え

## 1. [x] [B-001] インライン編集機能 (Inline Editing) - Done
- **概要**: ノードをダブルクリックすることで、その場でタイトル（ラベル）を編集できる機能。
- **目的**: Markdownファイルを直接書き換えなくても、キャンバス上で直感的に思考を具体化できるようにする。編集内容は Yjs を通じて `projectFiles` (spec.md/design.md) に書き戻す。

## 2. [ ] [B-002] 詳細表示パネル (Detail Panel)
- **概要**: 選択中のノードの ID に対応する Markdown セクションの全文を画面右側のサイドパネルに表示する機能。
- **目的**: 図面上の「ハコ」と、その背景にある「詳細な仕様・設計内容」をシームレスにリンクさせ、トレーサビリティを体現する。

## 3. [ ] [B-003] オートレイアウト中の手動位置微調整 (Manual Tweaks in Auto-Layout)
- **概要**: 自動レイアウトが ON の状態でも、特定のノードに対してユーザーが手動でオフセット（微調整）を加えられるようにする。
- **目的**: 基本的な整列はアルゴリズムに任せつつ、「このノードだけは少し離したい」といった人間の感性によるレイアウト調整を許容する。

## 4. [x] [B-004] 構造적ツリー編集ショートカット (Structural Tree Shortcuts) - Done
- **概要**: `Ctrl + 矢印キー` を使用して、フォーカス中のノードをツリー構造内で移動させる機能。
    - [x] **Enter / Ctrl + Up/Down**: 兄弟ノードの追加と順序入れ替え（Done: [S-016]）。
    - [x] **Ctrl + Right**: 上にある兄弟ノードの子ノードに移動（階層を下げる / Done）。
    - [x] **Ctrl + Left**: 親ノードの弟ノードに移動（階層を上げる / Done）。
- **ルール**: 移動時は子ノード（サブツリー）をすべて引き連れて移動する。
- **目的**: マウスを使わずに、アウトライナー感覚で図面の論理構造を高速に組み替えられるようにする。

## 5. [x] [B-005] ノードとノードの間のエッジをノードを２回クリックするだけで追加 - Done
- **概要**: エッジ編集モードを導入し、エッジ編集モードに入ったときには、Fromノードをクリック、Toノードをクリックすることでエッジを引ける機能。

## 6. [B-006] エッジ名のインライン編集
- **概要**: エッジをダブルクリックすることで、その場でタイトル（ラベル）を編集できる機能。

## 8. [x] [B-008] ノードの直接移動 - Done
- **機能**: ノードを今の親から切り離し、別のノードに近づけるとその子ノードに、エッジに近づけるとその間に割り込むように移動できる機能。
- **目的**: ノードの移動をハンドルでつけ変える操作は細かいノードをつかむためコストを抑えるため

## 9. [ ] [B-009] Undo 履歴の最適化 (Undo Atomicity)
- **概要**: エッジ追加（2クリック）や複雑な構造変更を、1回の Undo で元に戻せるようにトランザクションを設計し直す。
- **目的**: ユーザー操作の直感的な「一歩戻る」を実現する。

## 10. [x] [B-010] プロジェクト名の編集 - Done
- **概要**: プロジェクト名を編集できる機能。エクスポート時のデフォルト名としても利用される機能。

## 11. [B-011] ノードの作成直後の即時ノード名編集
- **概要**: ノードを追加した直後にアルファベットを入力するとインライン編集を開始する機能

## 12. [x] [B-012] ノードを上へ挿入機能 - Done
- **概要**: 選択しているノードの上にノードを追加する機能。Enterで挿入する位置の反対の動作。ノードが上にがある場合には選択ノードとの間に挿入する。

## 13. [x] [B-013] ノードを左へ挿入機能 - Done
- **概要**: 選択しているノードの左にノードを追加する機能。Insで挿入する位置の反対の動作。親ノードがある場合には選択ノードと親ノードのの間に挿入する。結果として親ノードと選択ノードのエッジは削除される。

## 14. [x] [B-014] レイアウトプレビュー (Ghost Node) - Done
- **概要**: ノード追加時に即座に配置するのではなく、マウスカーソルにノードが吸着した状態（プレビュー）にし、ユーザーが納得した地点でクリックして配置を確定させる機能。
- **目的**: 思考を中断せずに、配置場所を「探りながら」決定できる直感的なUIを提供し、自由な発想を支援する。

## 15. [ ] [B-015] 構造操作ロジックのリファクタリングとテスト強化
- **概要**: `onAddStructuredNode` や座標計算ロジックを純粋関数として抽出し、コンポーネントから独立させる。
- **目的**: 機能が複雑化しても安定性を維持し、「驚きを与えるような新機能」を迅速かつ安全に実装できる盤石な基礎を作る。

## 16. [x] [B-016] 選択ノードに関連するノードのみを表示
- **概要**: 選択しているノードに対してつながっているノードだけを一時的に表示でき、解除できる。左右どちらも、右側だけ、左側だけ、一つずつ展開、一つずつ折りたたむなどキー操作で見たいものだけをフィルター表示する機能。

## 18. [x] [B-018] MCPサーバー (Model Context Protocol) - Done
- **概要**: 外部の AI アシスタントが図面のノードやエッジを直接操作できる MCP サーバーを実装する。
- **目的**: AI に「この仕様に基づいて図面を更新して」「関連するノードを接続して」といった指示を出し、図面と AI の双方向連携を実現する。

## 19. [x] [B-019] インポート、エクスポート機能の改善 - Done
- **概要**: 外部連携しやすくするために、zipファイルでの入出力でなく１ファイルのテキストでできる機能

## 20. [ ] [B-020] 複数ノードの移動
- **概要**: 複数ノードを選択して一度に移動する機能

## 21. [ ] [B-021] エッジにノードをドラッグドロップ
- **概要**: エッジにノードをドラッグドロップしたらその間に挿入される機能（以前実装したと言っているけど全く実装されていないので備忘録で追記した）

## 22. [x] [B-022] 階層テキストのコピペ機能 - Done
- **概要**: クリップボードへコピーされたテキストからノードを追加、または選択ノードをテキストとしてコピーできる機能。
- **目的**: Excelやアウトライナー等、外部ツールとの迅速な情報交換を可能にする。

## 23. [ ] [B-023] Dangling Edges の自動削除
- **概要**: Markdown ファイルをインポートする際、`## Nodes` セクションに存在しないノード ID を参照しているエッジ（Dangling Edges）を自動的にフィルタリングし、削除する。
- **目的**: インポート後の図面データ整合性を確保し、無効なエッジがキャンバス上に残ることを防ぐ。
- **詳細**: `parseMarkdown` 関数でノードとエッジを生成した後、エッジの `source` および `target` が生成されたノードリストに存在するかを検証する。存在しない場合はそのエッジを破棄する。

## 24. [x] [B-024] 参加者全員の見ている場所を強制移動 - Done

## 25. [x] [B-025] プレゼンターのマウスポインタを共有 - Done
- **概要**: プレゼン中、プレゼンターがどこを指しているか分かるようにマウスカーソルを同期表示する機能。
- **概要**: 同じプロジェクトに参加している人の見ている位置、倍率を強制的に同期する機能

## 26. [x] [B-026] ツールバーのモダン化 (Toolbar Modernization) - Done
 - **概要**: 右側のツールアイコン群を、最新のツールのようにはしご状（隙間なし）に配置し、フローティング化や折りたたみが可能なインターフェースに刷新する。
 - **詳細**: 
     - [x] アイコンを隙間なく垂直にスタックし、境界線を最小化したデザイン。
     - [x] ツールバー全体を掴んで移動（フローティング）できる機能。
     - [x] ツールチップ、アイコン色、Undo/Redo無効化状態の完全復旧。
 - **目的**: ツール全体の質感を高め、プロフェッショナルな作業環境を提供する。

## 27. [x] [B-027] ツールバーアイコンの視認性改善 (Toolbar Icon Refinement) - Done
 - **概要**: アイコンのデザインを直感的なものに変更する。「ノードを追加」を四角、「エッジを追加」を矢印線にする。
 - **目的**: ツールチップを読まなくても機能が判別できるようにし、UXを向上させる。

## 28. [x] [B-030] Lucidchart 方式の操作体系の実装 - Done
 - **概要**: 左ドラッグで範囲選択、右ドラッグでパン（画面移動）を行う操作系への刷新。
 - **目的**: 編集とナビゲーションの切り替えコストを最小化する。

## 29. [x] [B-007/017/029] ノード属性管理と一括設定UI - Done
- **概要**: クラス情報をラベル文字列から分離し、`data.nodeClass` 属性として独立して保持。ツールバーからの設定・一括変更・バッジ表示を可能にする。
- **目的**: 属性ベースでのカラーリングを実現し、図面データの構造化と視認性を向上させる。

## 30. [x] [B-031] エッジ追加時のラバーバンド表示 (Rubber-band Edge) - Done
- **概要**: エッジ追加モードにおいて、始点ノード選択後、マウスカーソルを追従する仮の線を表示し、接続先をスナッププレビューする。
- **目的**: 接続操作の視覚的なフィードバックを向上させ、直感的なエッジ作成を支援する。

## 31. [x] [B-032] MCPサーバーの高度化 (マルチクライアント・属性操作) - Done
- **概要**: 
    - SSEトランスポートへの移行により、複数AIクライアントの同時接続を実現。
    - ツール群 (`update_node`, `delete_node`) の追加。
    - ツール引数への `nodeClass` 追加。
    - 開発環境での自動再起動 (`--watch`) の導入。
- **目的**: AIによる図面操作をより安定させ、属性管理を可能にする。

---
*※ これらの項目は順次 spec.md / design.md に昇格させ、実装に移行する。*